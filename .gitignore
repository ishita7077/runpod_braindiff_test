.DS_Store
node_modules/

.venv/
.pytest_cache/
__pycache__/
.ffmpeg-bin/
.pyinst/

# Runtime / generated artifacts
cache/
logs/
backend/startup_manifest.json

# Local datasets and training outputs
tribev2_data/
tribev2_save/

# Upstream dependency clone (managed separately)
tribev2/

# Local agent mistake log (not for git)
MISTAKES_AGENT.md

# Local tooling binaries
.tools/
.vercel
# Vercel pulls real production secrets into .env.production via `vercel env pull`.
# Never commit it — contains live BLOB_READ_WRITE_TOKEN, UPSTASH_*, TURNSTILE_*.
.env.production
.env*.local

# Results page generation outputs (per-comparison artefacts)
/frontend_new/outputs/
/audit_log/
/manual_overrides/

